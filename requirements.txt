fastapi==0.112.0
uvicorn==0.30.6            # no [standard]; pure-Python
pydantic==2.8.2

huggingface_hub[hf_transfer]==0.26.2
transformers==4.52.3       # Qwen2.5-Omni support from PyPI
accelerate>=0.33.0
safetensors>=0.4.3
einops>=0.7.0
sentencepiece>=0.2.0
numpy>=1.26

# Keep tiktoken out for now (can require rust). Add later if you need it.
# tiktoken>=0.7.0
